<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You for Shopping</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            min-height: 100vh;
            background: linear-gradient(135deg, #f5f7fa 0%, #fff 100%);
            font-family: system-ui, -apple-system, sans-serif;
        }

        .container {
            position: relative;
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .animation-container {
            position: relative;
            width: 100%;
            max-width: 400px;
            margin: 2rem auto;
            height: 120px;
        }

        .road {
            position: relative;
            width: 100%;
            height: 100px;
            background-color: #2F363F;
            border-radius: 50px;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .road-lines {
            position: absolute;
            display: flex;
            gap: 25px;
            top: 45px;
            left: -25px;
            animation: moveLines 2s infinite linear;
        }

        .line {
            width: 20px;
            height: 6px;
            background-color: #fff;
            border-radius: 3px;
        }

        @keyframes moveLines {
            from { transform: translateX(0); }
            to { transform: translateX(-50px); }
        }

        .bus {
            position: absolute;
            right: 110px;
            animation: moveBus 9s infinite linear;
            transform-origin: center;
        }

        @keyframes moveBus {
            0% { transform: translateX(200px) scale(0.8); opacity: 0; }
            20% { transform: translateX(0) scale(1); opacity: 1; }
            80% { transform: translateX(0) scale(1); opacity: 1; }
            100% { transform: translateX(-200px) scale(0.8); opacity: 0; }
        }

        .back {
            position: absolute;
            top: 20px;
            right: 0px;
            width: 100px;
            height: 60px;
            background-color: #3498db;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .front {
            position: absolute;
            top: 23px;
            right: -54px;
            width: 50px;
            height: 55px;
            background-color: #2980b9;
            border-radius: 0 10px 10px 0;
        }

        .gift {
            position: absolute;
            width: 35px;
            height: 35px;
            top: 35px;
            left: 20px;
            background: linear-gradient(45deg, #f1c40f, #f39c12);
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.2);
            animation: giftMove 9s infinite ease-in-out;
        }

        @keyframes giftMove {
            0% { transform: translateX(0) scale(1); }
            50% { transform: translateX(150px) scale(1.1); }
            100% { transform: translateX(400px) scale(0); }
        }

        .buttons-container {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 2rem;
        }

        .button {
            padding: 12px 24px;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-align: center;
            min-width: 200px;
        }

        .primary-button {
            background-color: #2980b9;
            color: white;
        }

        .primary-button:hover {
            background-color: #050b23;
            transform: translateY(-2px);
        }

        .secondary-button {
            background-color: #2980b9;
            color: white;
        }

        .secondary-button:hover {
            background-color: #050b23;
            transform: translateY(-2px);
        }

        .title {
            text-align: center;
            color: #2c3e50;
            font-size: 2.5rem;
            margin: 2rem 0;
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @media (max-width: 768px) {
            .container { padding: 15px; }
            .title { font-size: 2rem; }
            .buttons-container { flex-direction: column; }
            .button { width: 100%; }
        }

        @media (max-width: 480px) {
            .animation-container { max-width: 300px; }
            .title { font-size: 1.5rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">Thank you for choosing to shop with us!</h1>
        
        <div class="animation-container">
            <div class="road">
                <div class="road-lines">
                    <div class="line"></div>
                    <div class="line"></div>
                    <div class="line"></div>
                    <div class="line"></div>
                    <div class="line"></div>
                    <div class="line"></div>
                </div>
                <div class="bus">
                    <div class="back"></div>
                    <div class="front"></div>
                </div>
                <div class="gift"></div>
            </div>
        </div>

        <div class="buttons-container">
            <a href="/" class="button primary-button">Continue Shopping</a>
            <a href="/myorders" class="button secondary-button">View My Orders</a>
        </div>
    </div>
</body>
</html>